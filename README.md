Dockerfile project scaffold
===========================

Starting point for docker build project. Run build.sh to generate images from Dockerfiles in the tag/build subfolders, auto-tagged as:
```
repo/package:tag-build-suffix
```
